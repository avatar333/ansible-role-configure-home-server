{% for host in myhosts %}
# A Record
  local-data: "{{ host.hostname }}.thedarkarts.za.net. A {{ host.ipaddr }}"
# PTR Record
  local-data-ptr: "{{ host.ipaddr }} {{ host.hostname }}.thedarkarts.za.net."

{% endfor %}
